docker-machine create -d virtualbox machineName
docker-machine env machineName
@FOR /f "tokens=*" %i IN ('"C:\ProgramData\chocolatey\lib\docker-machine\bin\docker-machine.exe" env dev1') DO @%i
docker build -t fooImageName .

docker run -d -p
docker run -p 4000:80 fooImageName

docker-compose run web bash
docker-compose up -d (starts containers in background and leaves them running)

FROM python:3
ENV PYTHONUNBUFFERED 1
RUN mkdir /code
WORKDIR /code
ADD requirements.txt /code/
RUN pip install -r requirements.txt
ADD . /code/


heroku container:login
heroku container:push web (pushes current image)
heroku container:release web 
